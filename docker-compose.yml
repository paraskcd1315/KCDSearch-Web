services:
  kcdsearch:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kcdsearch
    restart: unless-stopped
    env_file: .env
    networks:
      - proxy-net

networks:
  proxy-net:
    external: true
